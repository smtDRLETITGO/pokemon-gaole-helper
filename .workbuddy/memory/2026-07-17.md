# 2026-07-17 工作記憶

## 00:45 官網數據來源架構確認（PING 指出 /uploads/images/ 含所有代別）
- 驗證：官網列表頁（cassette/N）`<li>` 只有 `<img>`+`<p>ID NAME</p>`，**無詳情頁、無 stats API、無 data 屬性**。
- 所有代別共用 `/uploads/images/<hash>.png` 主機，按 `cassette/N` 分頁索引（cassette/10 回 200，確認多代存在）。
- **官網能給**：cardId、名字、正面圖（模板比對）、版型（直/橫，官網已分 橫式/直式 卡匣）、星等（印在正面圖左下，金黃★）。
- **官網給不了**：六維數值（HP/攻/防/特攻/特防/速）——只印在實體卡**背面**，需 PING 拍背面照片。
- 結論：官網 = 身分+圖庫+版型+星等(從圖讀)；六維必須靠實卡背面。

## 星等像素計數是無底洞（實測）
- 高清明渲染圖星星有光暈/漸層，相鄰星像素黏連。
- 試過：連通元件(CC)、形態學 closing(合併更糟)、erosion 2px(2-2-001 變0★/2-2-026 變1★)、tight 閾值——均不穩，卡與卡差異大。
- 結論：星等偵測用「一次性 VLM 批次讀官方正面圖」或「從 PING 拍的背面圖讀」才靠譜；像素法只宜當 rough pre-screen（±1 星寬鬆匹配已容忍誤差）。

## 下一步建議（待 PING 決定）
- A: PING 拍全部 73 張背面 → 一次性 VLM 讀 星等+六維 → 全庫校正（最完整）。
- B: 一次性 VLM 讀官網正面圖星等（不需拍背面，解星等；六維仍缺）。
- C: 多代擴充：scraper 參數化 cassette/N，複用現有 reconcile 流程。

## 20:14 Phase 5.5 延續：橫式卡片星等全數校正 (Phase 5.6)
- **根因確認**：Phase 5.5 的 CV skeleton 法在 7 張橫式卡失敗（stars=0），1 張低信心錯誤（017:1★實4★）
- **原因**：橫式卡（aspect>1.05）的星星在左側中下部水平排列，CV 裁切區域只對直式卡的左下角有效
- **解法**：以 Read 工具視覺檢查全部 8 張 PNG → 手動修正 _cv_stars.json → merge 到 DB
- **校正結果**：002=5★, 003=6★(不變), 004=5★, 006=5★, 008=4★, 009=6★(不變), 017=4★, 023=5★
- **最終狀態：73/73 張卡星等齊全 ✓** 分佈: 2×11 / 3×19 / 4×7 / 5×15 / 6×21
- Commit `fc8bc2e` push 到 main → GitHub Pages 自動部署

## 20:30 CV 腳本改善：增加橫式卡版型偵測
- 改進 `scripts/count_stars_cv.py`：
  - 新增 `detect_layout()` 依 aspect ratio 分直式/橫式
  - 新增 `find_horizontal_star_row()` 用 Y 軸投影自適應找到橫式卡星等行位置
  - 新增方法C（輪廓計數）和方法D（X軸投影峰值）作為橫式卡輔助候選
- **實測結果**：橫式卡準確度因藝術元素干擾仍有限（~60%）；直式卡保持 ~95%+
- **結論**：CV 自動偵測適合做 first pass（~85% 整體準確度），邊際案例需視覺校正
- Commit `2016bf8` 已 push

### 待辦（高優先）
- ~~六維數值（hp/atk/def/spAtk/spDef/spd）73 張全為舊 DB 值，需實卡背面 OCR 校正~~ → **已完成！**

## 01:45 Phase 5.7 — 背面圖六維 OCR 全校正 ✅

**重大更正**：PING 提供官網背面圖 URL 證據 → 確認「官網有背面圖含六維」
- 之前「官網不提供背面圖/需拍實卡」是錯誤假設，已更正

完成事項：
1. **背面圖來源**：cassette/N 頁面 Nuxt payload 含 `back_image` 欄位
2. **Agent 抓取**：extract-backs.mjs 下載 73 張背面圖 → public/cards/11/back/
3. **easyocr OCR 管線**（scripts/ocr_back_stats.py）：
   - easyocr 讀文字→篩選數字→Y/X 排序映射六維
   - 修正 API 格式搞反 (bbox,text,conf)、移除去重、Y/X 決勝
   - 測試 18/18=100%，批量 73/73=100%，2m18s
4. **合併 DB**：71 張修正 + 8 張 null 填入 = 73/73
5. **驗證**：2-2-026 OCR(HP103/攻72) 與 PING 實卡完全一致 ✅
6. Commit `0ab6241` pushed → GitHub Pages deployed

## 01:45+ Phase 5.8 — 屬性/招式/攻擊型態 全量驗證（73卡逐一讀圖）

**觸發**：PING 審閱 DB 發現 type1/type2/moveName/moveType/moveCategory 大量錯誤，要求「沒有正確之前不要輕易推送」。

**方法**：用 Read 工具（多模態）逐一讀取全部 73 張背面圖 PNG，辨識：
- 招式名稱（背面圖中央文字 + 屬性圖示）
- 招式屬性（冰/草/火/水/電/鋼/妖精 等 icon）
- 攻擊型態（紅色圓點=物理 / 無標記=特殊）
- 寶可夢屬性（採 canonical Pokédex 數據，因背面圖未明確顯示）

**發現**：
1. **完全正確僅 ~11 張**：007 烈空坐、008 拉帝亞斯、009 拉帝歐斯、010 四顎針龍、011 巨金怪、018 具甲武者、019 君主蛇、020 炎武王、021 大劍鬼、031 狂歡浪舞鴨、032 藤藤蛇
2. **058–065 重災區**：8 張卡的 type/move 全部系統性錯位（疑似建庫索引偏移）
3. **035–057 大規模漂移**：進化鏈屬性被錯誤填入其他寶可夢數值
4. **★4~★6 及 R-Special 卡缺第二招**：DB schema 僅單 moveName 欄位，實際有 2 招
5. **066–070 + R 卡 null 已提取**：從背面圖讀出實際值待寫入

**產出**：`scripts/_verification_report_5.8.html` — 完整 73 卡比對表
**狀態**：⏸️ **尚未修改 DB 或推送**，等 PING 確認

## 07:53 星塵第4彈 (cassette/9) 完整建庫 — 76/76 卡 ✅

**觸發**：PING 要求「星塵第４彈 也要補上」

**完成事項**：
1. **Nuxt payload 解析**（已有）：`_backs_9_norm.json` — 76 張卡（含 3 張特卡：夢幻/蒼響/藏瑪然特 + 3 張 R 卡）
2. **圖片下載**（已有）：`public/cards/9/` 76 正面 + `public/cards/9/back/` 76 背面
3. **六維 OCR**（已有）：`_ocr_stardust4_stats.json` — easyocr 76/76=100%
4. **GOLD 元數據提取**（本次）：
   - Read 工具逐一讀取全部 76 張背面圖 → 提取星等/版型/type1/type2/move*/category
   - OpenRouter VLM API 失敗（free model 超時），改用 Read 工具直接讀圖
   - 寫入 `scripts/_vlm_stardust4.json`（76 records, build_stardust4_db.mjs 預期格式）
5. **DB 生成**：`build_stardust4_db.mjs` → `src/data/pokemonDb.cards.stardust4.generated.js`
   - 76/76 cards, 0 issues
   - 星等分佈：★2×14 / ★3×14 / ★4×35 / ★5×13
   - 雙招式卡：13 張 | 雙屬性：26 張 / 單屬性：50 張
6. **Build 驗證**：`vite build` 通過 (29 modules, 3.43s)
7. **驗證頁**：`scripts/verify_stardust4.html` — 待 PING 審閱
8. **generations.js 已註冊**：stardust4 entry 已在 prior session 啟用

## 16:41 Phase 5.9 — 星等全面校正 (Galaxy Vol.2 / cassette/11) ✅

**觸發**：PING 審閱 verify_galaxy2.html 發現 2-2-029 星等錯誤 (DB:6★, 實際:4★)，要求「再想想辦法」。

**嘗試過的 CV 方法（全部失敗）**：
1. count_stars_v4.py — contour counting：水平卡星暈合併成 1 blob → undercount
2. DT (Distance Transform) local maxima：同上，merged blob = 1 center
3. X-projection peaks：水平卡 merged → 1 peak；垂直卡 ROI 太大含寶可夢藝術 → overcount
4. Erosion split：水平卡 erode 後 area filter 過嚴 → 0 blobs
5. **結論**：CV 對 MEZASTAR 背面圖星等不可靠（金暈黏連 + 背景雜訊），人工讀圖是唯一金標準

**最終解法**：逐一 Read 全部 73 張 `public/cards/11_small/*.jpg` 背面圖，目視確認 ★ 數量。

**結果**：**31/73 (42%) 卡片星等錯誤**，已全部修正：
| 修正後 | 卡數 |
|--------|------|
| 6★ | 2 (蒼響001、藏瑪然特002) |
| 5★ | 24 |
| 4★ | 18 |
| 3★ | 14 |
| 2★ | 12 |
| 1★ | 3 (R-2-1 皮卡丘, R-2-2 噴火龍, R-2-3 耿鬼) |

**主要修正**：鋁鋼龍6→5, 化石翼龍2→5, 雙斧戰龍3→5, 魔幻假面喵6→4, 骨紋巨聲鱵6→4, 百足蜈蚣6→2(最大誤差), 沙奈朵6→4, 艾路雷朵6→4, R系列全部改為1★ ...共31張
**完整清單**：`scripts/_star_ground_truth_galaxy2.json`

**狀態**：Build 通過 ✓, verify_galaxy2.html 已重新產生, ⏸️ 待 PING 確認後推送

## 19:48 資料重建原則確立 + 銀河2 乾淨模板落成
**PING 明確要求（重要方法論）**：不要執著舊 DB 的自動辨識初值，掃描圖才是唯一權威來源；資料檔要「從圖重新長出」，不是去補舊的錯值。
**證據**：銀河1 / 星塵4 星等分佈異常（無 1★ 也無 6★，大量卡卡 4★）→ 仍背著有 bug 的自動星等初值，必須重建。
**已落成銀河2 乾淨模板（pipeline）**：
- `scripts/galaxy2.ground_truth.json` = 圖萃取真值（cardId/name 官網列表、stars 人工複讀73/73、六維 easyocr、type/move VLM），含 provenance 註記
- `scripts/gen_galaxy2.mjs` = 生成器，只從 ground-truth JSON 重生成 `src/data/pokemonDb.cards.generated.js`
- `.generated.js` 視為「生成物」，文件頭標註「勿直接手改」；改資料→改 JSON→重跑生成器
- 驗證：星等分布 6★×2/5★×24/4★×18/3★×14/2★×12/1★×3；11 張重點卡 spot-check 全 PASS；vite build 通過；verify_galaxy2.html 重產
**待辦**：銀河1(cassette/10)+星塵4(cassette/9) 需同法重建（星等仍錯）；未來新代別統一走 ground-truth JSON→生成器 管線。

## 21:28 Phase 5.10 — AGY/Gemini VLM 辨識系統建立（進行中）

**觸發**：PING 指出 2-2-004 噴火龍我的「人工複讀」也錯了：
- 我記 stars=5★, type1=火, hp=166 → 實際 **stars=6★, type1=龍(待確認), hp_front=150**
- 而且漏掉「超級明星」標記和「極巨化符號」

**核心發現：我的 Read 視覺不可靠，不能當校正基準。**

**解法**：用 AGY (Antigravity CLI + Gemini VLM) 當辨識引擎：
- **測試結果（已驗證）**：
  - 2-2-004 噴火龍：AGY 正確讀出 6★ / HP150 / 超級明星✓ / 極巨化✓
  - 2-2-029 骨紋巨聲鱵：AGY 完全匹配 PING 的每一個欄位值（4★/火幽靈/禍不單行/HP157/六維全對）
- **策略**：兩階段辨識 — 正面圖(星等/name/type/hp/layout/特殊標記) + 背面圖(六維/招式)
- **管線腳本**：`scripts/agy_recognize.py` v2 — 批次 73 卡，resume-friendly，自動 merge→ground_truth.json
- **狀態**：批次執行中（background task eMyul1），73 張 × 2 calls = ~146 次 AGY 呼叫

## 21:52 Prompt 升級：符號增強辨識（進行中）

**PING 提供**：
- MEZASTAR 18 屬性符號對照表 → 存為 `scripts/_ref_type_chart.jpg`
- 要求 prompt 必須包含符號/圖示的辨識指令
- 更正：PING 沒有說噴火龍是龍+飛行（我之前記錯了）

**Prompt 升級內容**（`agy_recognize.py` FRONT_PROMPT / BACK_PROMPT）：
- 內建 18 種屬性圖示描述（顏色+形狀）：一般/格鬥/毒/地面/飛行/蟲/岩石/幽靈/鋼/火/水/電/草/冰/超能力/龍/惡/妖精
- 正面圖新增：hasSuperStar / hasGigantamax 符號偵測
- 背面圖新增：moveType 從 18 種選（不再自由發揮）、moveCategory 明確 D/X/S 對應
- type1/type2 要求根據卡片上的**圖示顏色+形狀**辨識

**計劃 → 已執行（22:00）**：
- 舊批次 eMyul1（舊 prompt + 原圖正面）跑了 13 分只出 3 張，且 2-2-003 正面逾時 → **已停掉**
- **模型確認**：gemini_boundary.json 中 generic_code 的 default_model = **flash**（Gemini Flash，省額模式），非 Pro。每張結果 front_model/back_model 欄位印 "flash" 可證。Flash 在 2-2-004/2-2-029 測試已全欄位正確，精準度夠。
- **腳本修正**（`agy_recognize.py`）：
  1. FRONT_DIR 改為 `11_small`（小圖正面 png ~250KB，原圖 `11/` 約 900KB 會觸發 AGY 90s 逾時）
  2. 新增 `prep_image()` 降採樣到最長邊 1024px（PIL）當保險
  3. call_agy timeout 90→150s、MAX_RETRIES 2→3、RATE_LIMIT 2→5s（對 AGY 溫柔）
- **重啟全量批次**：task `Mrw8Z0`（--force，73 張全重跑，含符號增強 prompt）。AGY 後端目前延遲飄忽（2-2-003 背面曾卡 3.5 分），靠 3 次重試磨過去。
- **待批次完成**：merge → galaxy2.ground_truth.json → gen_galaxy2.mjs → 重生成 .generated.js → build → verify 頁 → 給 PING 確認（信任規則，未確認不推送）。
- 失敗卡（ok:false）會有下一輪用 --model pro 補跑（Pro quota 200/日，足 Cover 少量失敗）。

## 22:20 Phase 5.11 — OpenRouter Gemma 4 31B 備援整合 ✅

**PING 提供**：OpenRouter 免費模型 `google/gemma-4-31b-it:free`（key 存 `.env`，已加進 `.gitignore`，**不進倉庫**）。
- 模型確認（OpenRouter /api/v1/models）：slug= `google/gemma-4-31b-it` / `:free`；多模態 `['image','text','video']` 能讀圖；ctx=262144。
- **設計**：AGY/Gemini Flash 主用 → 同一張卡若 AGY 重試 3 次仍失敗 → 自動切 OpenRouter Gemma 4 31B 備援（結果標 `front_fallback`/`back_fallback`，merge 時寫入 `_meta`）。
- **關鍵差異**：AGY 的 gemini_gate 能讀本地檔案路徑，OpenRouter 不能 → `call_openrouter()` 改為 base64 內嵌圖片送 API（urllib，stdlib，無額外依賴）。
- **已加 429/5xx 自動重試**（免費檔上游常限流）：call_openrouter 內 1 試 + 2 重試（8s/16s backoff）。
- **冒煙測試**：連上 OpenRouter、鑑權通過、圖已送出、結構化錯誤可解析 ✓；當下 `:free` 檔被上游 429 限流（免費檔常態），成功路徑 JSON 解析與 AGY 同源已驗證可用。
- **22:30 首測更新**：純文字請求成功（"strawberry"=3 個 r，證 key/model/auth/回應解析全通）；但**帶圖視覺請求在等 60s 後仍 429** → 免費檔的 vision 被上游持續限流，無法可靠讀卡。結論：**文本可用、視覺不可用**。若需視覺備援穩定，須切付費檔 `google/gemma-4-31b-it`（設 env `OPENROUTER_MODEL`）。AGY Flash 主線不受影響。
- 模型可切付費檔 `google/gemma-4-31b-it`（設 env `OPENROUTER_MODEL`）確保備援穩定，但 PING 指定用免費檔。
- **用法**：`python scripts/agy_recognize.py`（resume+repair 模式已改為「ok 狀態感知」：只重跑 missing/partial 卡，不再只看檔案存在）。
- **注意**：Mrw8Z0 仍在跑舊腳本（無備援），不重啟以免併發寫同一 result 檔；批次完成後用新腳本跑 repair pass（partial 卡重試 AGY→Gemma）即可。

## 22:34 Phase 5.12 — OpenRouter streaming 測試 + AGY 星等異常觀察
- **PING 貼 OpenRouter streaming curl（帶 -N 看流式分塊）** → 實測**純文字 stream 也 429**（前 10 分鐘文字還通，現整池枯竭）。
- **結論**：`google/gemma-4-31b-it:free` 走 OpenRouter **共享上游配額**，現已被耗盡；streaming / vision / reasoning 參數都**不能**繞過 429。
- 錯誤訊息自帶解法：`add your own key to accumulate your rate limits` → (a) BYOK 綁 Google AI Studio key，或 (b) 切付費檔 `google/gemma-4-31b-it`（走 PING OpenRouter 額度，不在共享池）。**未經 PING 同意不切付費檔、不花額度**。
- **AGY 星等潛在過讀警訊**：Mrw8Z0 跑到 14/73 時，2-2-003~009 多張 AGY 讀成 6★；但 Phase 5.9 人工目讀基線全代僅 **2 張 6★**（蒼響001/藏瑪然特002）。高度疑似 AGY 把「超級明星」字樣或星芒特效數多 → **星等可能 over-count**。
- **對策（不中斷批次）**：merge 後做「AGY 星等 vs Phase5.9 人工基線」差異比對，衝突卡交 PING 對實圖裁定；符合「圖為唯一權威 + 未確認不推送」規則。
- 批次 ETA ~1.5h（1.8min/張）。

## 22:40 PING 決定：免費檔有問題，先等 FLASH 跑完
- **決策**：不切付費檔 `google/gemma-4-31b-it`、不綁 BYOK、不花 OpenRouter 額度。Gemma `:free` 備援維持現狀（限流時形同不可用），當「將來不限流再啟用」的備胎。
- **當前唯一工作**：等 Mrw8Z0（AGY Flash 73 張全量）跑完 → 自動通知 → merge → AGY星等 vs Phase5.9人工基線差異比對 → 驗證頁 → PING 對實圖裁定衝突 → 確認後才推送。

## 後續（session resume）— 預覽伺服器修復 + SPECIAL 卡確認
- **問題**：port 8080 上有兩個衝突的 `http.server`（一個 serve 專案根目錄、一個 serve /c/tmp_serve），且 python 子程序 (PID 118092) 在 kill bash wrapper 後殘留 → `http://localhost:8080/pokemon-gaole-helper/` 回 404，PING 無法視覺驗證。
- **修復**：PowerShell 強制清空 8080 → 重新 `vite build`（29 modules, 0 error）→ `cp -r dist /c/tmp_serve/pokemon-gaole-helper` → 單一 `python -m http.server 8080` 從 `/c/tmp_serve` 啟動（background task GGkb7p）。
- **驗證**：`/pokemon-gaole-helper/` + assets + `cards/11/2-2-001.png` 全部 HTTP 200。
- **SPECIAL 卡確認**（在重建後的 `.generated.js`）：`"category": "special"` 計數 = 3（R-2-1 皮卡丘 / R-2-2 噴火龍 / R-2-3 耿鬼），三張均 `stars:0` ✓；星等分布與官網一致（6★×10/5★×15/4★×17/3★×14/2★×14/SPECIAL×3）。
- **狀態**：預覽已可視覺驗證；**尚未 commit/push**（遵循信任規則「未確認不推送」），等 PING 核准推送。

## PING 核准推送（10:0x）
- PING 視覺確認預覽無誤 → 核准推送。同時問「app script 有需要改的東西嗎？」
- **發現 GAS backend 需要改**：`backend/google_apps_script.js` 的 `syncCollection` 原 `card.stars || 1` 會把 SPECIAL 卡(stars:0) 強轉成 1★，且 Sheet 無 category 欄 → sync round-trip 失真。
- **修正 GAS**：special 卡 stars 欄寫 `"SPECIAL"` 字串 + 新增 `category` 欄（含既有 Sheet 遷移補 header）；`App.jsx handleFetchCollection` 偵測 `category==='special' || stars==='SPECIAL'` 還原 `stars:0, category:'special'`。
- **推送範圍決策（信任規則）**：只推 **銀河第二彈（galaxy2）SPECIAL 修復**。`generations.js` 已 `git checkout HEAD` 還原成只註冊 galaxy2（HEAD 狀態），**不放行 galaxy1/stardust4**——因其星等仍背舊自動初值（無 1★/6★，分佈異常），須 AGY 重建後再推，避免發佈錯誤資料。
- **commit `594d2af`**（8 files: generated.js/App.jsx/CardAlbum/CardRegister/pokemonDb.js/index.css/backend GAS/.gitignore）→ `git push origin main` 成功 → GitHub Pages 自動 build+deploy。
- **未提交（刻意排除）**：worklog.md(galaxy1 條目)、_star_debug/*.png、scripts/* 除錯檔、public/cards/9+10、galaxy1/stardust4 生成檔、generations.js 改動——全部留本地。
- **⚠️ 使用者手動步驟**：`backend/google_apps_script.js` 改了，但 GAS 是部署在 Google 帳號的 web app，**須 PING 手動把新程式碼貼進 Apps Script 編輯器並 redeploy**，Sheets 同步的 SPECIAL 修復才生效。repo 推送不會自動更新 GAS。
- 線上部署網址：https://smtDRLETITGO.github.io/pokemon-gaole-helper/
