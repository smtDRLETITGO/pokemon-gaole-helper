# 2026-07-16 — Pokémon MEZASTAR 辨識重構（WorkBuddy 接手段）

## 完成工作
- Phase 2：重構 `src/components/CardRegister.jsx` 的 `captureAndOCR`，辨識管線改為「模板比對（主，離線 aHash+dHash）≥0.80 直接命中；0.55–0.80 卡號 OCR 確認；否則降級 QR 快取 → VLM」。`switchTab` 進 ocr 頁預熱 `ensureReferenceHashes()`。修復 GAS 代理 OCR 未受 `!resultObj` 閘控會覆蓋模板命中結果的 bug。
- Phase 3：確認 `ScreenOcr.jsx` 保留 VLM-only 機台敵卡路徑（不動）。
- Phase 4：凍結 `backend/google_apps_script.js` 的 `checkQr`/`saveQr`/`QR_CACHE`（加 FROZEN 註解，說明背面 QR 機台專用掃不到）；前端對應 QR 路徑加凍結註解。確認 `tesseract.js` 已移除、`QrScanner.jsx` 不存在。
- **修復 Phase 1 遺留 bug**：`pokemonDb.js` 未 re-export `PRESET_POKEMON_DB`，導致 build 報 `MISSING_EXPORT`；補 `export { PRESET_POKEMON_DB };`。`npm run build` 現通過（首次完整建置成功）。
- 依使用者要求 append Phase 2/3/4 三筆 entry 到 `worklog.md`（供 Antigravity 無縫接手）。

## 關鍵決策 / 注意
- 模板比對信心閾值 0.80/0.55 為初版經驗值，實機測試後可能需下調至 ~0.70。
- 模板比對目前只涵蓋銀河第二彈（SERIES_DIR='11'）；多代數需參數化 `cardTemplateMatcher.js`。
- 8 張 `needsStats` 卡（2-2-066~070、R-2-1/2/3）仍缺數值，待補。
- 時鐘環境不一致：系統 current_time 顯示 20:14，但 worklog 既有 Phase 0/1 為 22:13/22:20；為維持 handoff 時間軸連續，新 entry 續用 22:30/35/40。

---
23:21 部署：git push origin/main (commit a2d7dfd)，Actions 自動 build+deploy 到 gh-pages。線上 https://smtDRLETITGO.github.io/pokemon-gaole-helper/ 約1-2分後更新。未推 .workbuddy/ 與 _probe_cassette11.html。

---
00:13 Phase 5 完成：星等偵測（countStarsByTips 數上尖端）+ 管線重構。commit 731ad6e 已 push。
⚠️ CRITICAL DB 異常：PING 實拍 2-2-026 背面驗證，舊 DB 的 stars(4→實3)、hp(135→103)、attack(90→72) 等全部錯誤。73 張卡全庫需重新以實卡校正。已寫入 worklog CRITICAL 段落。
